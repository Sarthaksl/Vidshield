# ─── Core ───────────────────────────────────────────────────────────────────
python==3.10
numpy==1.26.4
pandas==2.2.2
Pillow==10.3.0
scipy==1.13.1
tqdm==4.66.4

# ─── Deep Learning ──────────────────────────────────────────────────────────
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0
timm==1.0.3                    # ConvNeXt-tiny backbone

# ─── Video / Vision Transformer ─────────────────────────────────────────────
mmcv==2.1.0                    # Required by mmaction2
mmaction2==1.2.0               # Video Swin Transformer backbone
einops==0.8.0                  # Tensor rearrangement used by Swin

# ─── CLIP + BLIP (Protection layer 1 + caption eval) ────────────────────────
open-clip-torch==2.24.0        # CLIP loss for UAP training + CLIP eval
transformers==4.41.2           # BLIP caption drift evaluation
salesforce-lavis==1.0.2        # BLIP model (alternative to transformers BLIP)

# ─── DCT / Frequency Domain (Protection layer 2) ────────────────────────────
# scipy.fft is used — already covered by scipy above

# ─── C2PA Provenance (Protection layer 3) ───────────────────────────────────
c2pa-python==0.5.0             # C2PA v1.3 manifest generation + verification

# ─── Cryptography (Fernet watermarking — v1 protection) ─────────────────────
cryptography==42.0.8           # Fernet symmetric encryption

# ─── Flask (Fernet tamper simulation web app — v1 protection) ───────────────
Flask==3.0.3
Werkzeug==3.0.3

# ─── Computer Vision Utilities ───────────────────────────────────────────────
opencv-python==4.10.0.84
albumentations==1.4.8          # Augmentation pipeline (JPEG, blur, dropout, etc.)

# ─── GradCAM ────────────────────────────────────────────────────────────────
grad-cam==1.5.2                # pytorch-grad-cam for saliency visualisations

# ─── Evaluation / Metrics ───────────────────────────────────────────────────
scikit-learn==1.5.0            # AUC, F1, confusion matrix
matplotlib==3.9.0
seaborn==0.13.2

# ─── Notebook ───────────────────────────────────────────────────────────────
jupyter==1.0.0
ipykernel==6.29.4